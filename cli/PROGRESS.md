# PROGRESS - cli/ npm-packaging loop

Control de iteraciones de este loop. Ver `.loop/HANDOFF.md` para el objetivo/guardarraíles vigentes y el protocolo Maker/Grader. Distinto del `PROGRESS.md`/`.loop/` de la raíz del repo (proyecto de Skills, ya cerrado).

## Done

- [x] Setup inicial del framework de loop engineering (`cli/.loop/`, `verify.sh`, guardarraíles) - ver `.loop/HANDOFF.md`. Incluye una excepción de una sola vez, ya ejecutada y documentada en `HANDOFF.md` § Paths: instalar hooks `PreCompact`/`SessionStart(compact)` en `.claude/settings.json` de la raíz del repo (fuera de `cli/**`) para respaldar `cli/.loop/HANDOFF.md` en compactaciones - agregado a los arrays existentes del loop de la raíz, sin reemplazarlos.
- [x] CLI construido y validado end-to-end contra la cuenta real (commit baseline `38f7d14`) - esto es la base sobre la que corre este loop, no un ítem de este loop en sí
- [x] **Validación final** (Ciclo 5): `npm pack` real (no `--dry-run`, genera el `.tgz` local, no publica a ningún registro) -> instalado con `npm install <tarball>` en un directorio limpio fuera de este repo (`npm init -y` + install local, sin tocar el `npm link` global existente) -> `node_modules/tiktok-cli/` confirmado sin `test/` ni `.loop/` -> corrido `tiktok --help` y `tiktok schema` con `XDG_CONFIG_HOME` apuntando a un directorio vacío nuevo y `TIKTOK_CLI_ENV_PATH` sin setear (simula una máquina nueva): ambos limpios, sin ninguna mención a este repo. `tiktok profile get` (requiere credenciales) falló correctamente con `AUTH_MISSING` apuntando a `tiktok auth login`, **nunca** leyó el `.env` real de este repo. Tarball y directorio de prueba borrados después, no quedó nada del test en el repo.
- [x] **Corrección post-Grader Ciclo 4**: el Grader de ese ciclo encontró, de paso (VERDE igual, no bloqueaba ese ítem puntual), dos referencias reales a "this repo" que un usuario externo vería: `package.json.description` (texto original desde el commit baseline, ningún ciclo anterior lo había tocado) y el banner de `tiktok --help` en `bin/tiktok.js` (que además tenía un path roto, `cli/skills/...`, con un prefijo `cli/` que no existe una vez instalado el paquete). Ambos corregidos. Confirmado con `tiktok --help` real que el banner ya no menciona el repo y el path del manual es el correcto para el paquete instalado.
- [x] **Manual para un usuario externo** (Ciclo 4): `skills/tiktok-cli/SKILL.md` reescrito - sacadas todas las referencias a "este repo" (`.env` de la raíz, `.loop/HANDOFF.md`, `npm link` como único install), reemplazadas por: instalación real (`npm install -g tiktok-cli` / `npx`), una sección nueva "Before you start" explicando que cada usuario trae su propia TikTok Developer App, credenciales en `${XDG_CONFIG_HOME:-~/.config}/tiktok-cli/.env`, y el hallazgo de `unaudited_client_can_only_post_to_private_accounts` inlineado (antes apuntaba a un archivo que no viaja en el paquete). Agregado `README.md` (nuevo, no existía) como landing page corta para la página de npm, con link al `SKILL.md` para el detalle completo - confirmado con `npm pack --dry-run` que ambos (`README.md`, `LICENSE`) se incluyen automáticamente sin estar en `files`. `verify.sh` verde.
- [x] **ESLint como devDependency** (Ciclo 3 de entrenamiento): `eslint@^10.9.1` instalado únicamente en `devDependencies` de `cli/package.json` (confirmado: no aparece en `dependencies`, y `npm pack --dry-run` no incluye `node_modules/`, `package-lock.json` ni `eslint.config.js` en el tarball - `files` sigue acotado a `bin/src/skills`). Config mínima manual en `eslint.config.js` (flat config, sin `@eslint/js` para no sumar otra dependencia - globals de Node listados a mano, sólo los realmente usados). `verify.sh` corrió el paso `lint:eslint` por primera vez como bloqueante real (antes era no-bloqueante por falta de instalación) y encontró 3 problemas reales de primera: un import sin usar en `auth/exchange.js` (quedó de un refactor de esta misma sesión), un `readdir` sin usar en un test, y un `== null` en `audit-log.js`. Los dos primeros se corrigieron en el código. El tercero es un idiom deliberado (`obj == null` cubre `null`+`undefined` en una sola comparación) - en vez de reescribirlo más verboso, se configuró `eqeqeq: ["error", "smart"]` (modo estándar de ESLint que permite específicamente ese caso, sigue marcando cualquier otro `==`/`!=` suelto). Decisión documentada acá para que el Grader la evalúe, no ocultada.
- [x] **package.json publicable** (Ciclo 2 de entrenamiento): `license: MIT` (`LICENSE` agregado, texto completo), `author: "NaruXT"` (sin email, decisión explícita del usuario), `repository` apuntando al monorepo con `directory: "cli"`, `keywords`, `"private": true` eliminado. Agregado `files: ["bin","src","skills"]` - confirmado con `npm pack --dry-run` que el tarball resultante (34 archivos, 19.1kB) incluye exactamente `bin/`, `src/`, `skills/`, `package.json`, `LICENSE`, y excluye `test/`, `.loop/`, `PROGRESS.md`, `friction.md`. `verify.sh` verde.
- [x] **Config portátil** (Ciclo 1 de entrenamiento): `src/lib/env.js` ya no calcula una ruta relativa al repo (`REPO_ROOT` eliminado). Ahora resuelve `TIKTOK_CLI_ENV_PATH` (override explícito) -> `${XDG_CONFIG_HOME:-~/.config}/tiktok-cli/.env` (default portátil). 2 tests nuevos en `test/env-path.test.js`, `verify.sh` verde (21 tests totales). **Hallazgo real, no un detalle menor**: esto cambia el comportamiento de este mismo repo hacia adelante - hasta ahora `tiktok` "simplemente andaba" corriendo desde acá porque encontraba el `.env` de la raíz solo; a partir de este cambio, seguir usando ese `.env` real en este repo requiere `export TIKTOK_CLI_ENV_PATH=$(pwd)/../.env` (o copiarlo a la ruta XDG nueva) - decisión del usuario, no tomada unilateralmente acá, no se migró/copió ningún credential real todavía.

## In progress

- Ninguna

## Next
- [ ] **verify.sh real**: lint (`node --check` en cada `.js` + ESLint), build/empaquetado (`npm pack --dry-run`), test (`npm test`), más el grep del guardarraíl `SELF_ONLY` en `post/direct.js`.

## Blocked

- Ninguna

- [x] **Corrección post-Grader final**: el Grader de cierre (revisión de las 6 condiciones completas, no un ciclo puntual) encontró un tercer caso del mismo patrón que los dos anteriores - el hint de error de `unaudited_client_can_only_post_to_private_accounts` en `src/commands/post/direct.js` apuntaba a `skills/tiktok-content-posting-api/SKILL.md`, un archivo que vive en la raíz del monorepo (proyecto de Skills, ya cerrado) y que no viaja en el paquete npm (`files` solo incluye `skills/tiktok-cli/`). Corregido: el hint ya no referencia ningún archivo, describe el hallazgo inline (mismo criterio que ya se había aplicado en el resto del `SKILL.md`). `verify.sh` verde.

## Notes

- Objetivo explícitamente NO incluye correr `npm publish` real - ver guardarraíl de irreversibilidad en `.loop/HANDOFF.md`.
- **Las 6 condiciones del objetivo están cumplidas** (Grader de cierre, subagente separado, en frío: VERDE 6/6, con evidencia propia para cada condición - ver `.loop/HANDOFF.md` § Estado).
